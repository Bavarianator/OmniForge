# Meson entry for GNOME / Flatpak integration (expand with gtk4 + rust module).
project('omniforge', 'rust', version: '0.1.0')
message('Meson scaffolding — wire rust.bindgen + cargo module for production builds.')
