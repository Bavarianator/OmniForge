//! Drag-and-drop ingestion UI (DropTarget wiring lands with `file_drop_zone`).

/// Placeholder until the Relm4 `Component` for Data Forge is implemented.
#[derive(Debug, Default)]
pub struct DataForgeViewPlaceholder;
